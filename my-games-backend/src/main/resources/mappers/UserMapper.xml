<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.octopus.mygamesbackend.mapper.UserMapper">
    <insert id="addUser">
        insert into t_account(username, password, create_time)
        values (#{username}, #{password}, #{createTime})
    </insert>
    <select id="getPasswordByUsername" resultType="String">
        select password
        from t_account
        where username = #{username}
    </select>
</mapper>